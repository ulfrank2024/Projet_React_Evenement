import Formulaire from "./components_contacct/Formulaire";
import Apropos from "./components_contacct/Apropos";

export default function Contact() {
    return (
     <div>
      {/* <Apropos /> */}
      <Formulaire />
     </div>
    );
}